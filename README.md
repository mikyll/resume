# resume

Michele Righi's resume.

## Directory Structure

Resume PDFs are placed in `output/` and have the follwoing naming convention:

## Create New Tag

```bash
git tag $(date "+%Y-%m-%d")
git tag latest
git push --tags
```
